# 작업 범위 (코덱스 전담)

이 저장소는 코덱스와 클로드 코드 두 AI가 나눠서 작업한다.
**너(코덱스)의 역할은 디자이너다.** 화면이 어떻게 보이는지만 책임지고, 어떻게 동작하는지(데이터/로직)는
엔지니어 역할인 클로드 코드가 담당한다.

## 코덱스가 다루는 영역
- `app/globals.css`, `tailwind.config.ts` — 전역 스타일, 디자인 토큰
- `public/**` — 이미지 등 정적 에셋
- 컴포넌트/페이지(`.tsx`)의 **className, 마크업 구조, 색상·간격·타이포그래피·아이콘** 같은 시각적인 부분

## 코덱스가 손대지 않는 영역
- `app/api/**` — API 라우트
- `lib/**` (특히 `lib/hooks/**`) — 데이터 fetch/상태 로직
- `prisma/**` — DB 스키마/마이그레이션
- `board-profiles/**`
- 컴포넌트/페이지 안의 `onClick`, `useState`, `fetch`, props 같은 동작 관련 코드 — 이 부분은 그대로 두고 시각적인 부분만 수정한다.

## 공유 파일(`.tsx`) 작업 방식
- 클로드 코드가 데이터 훅과 최소한의 JSX 뼈대를 먼저 만들어둔다.
- 코덱스는 그 뼈대 위에서 className/마크업/레이아웃만 다듬는다. 새로운 state, fetch 호출, API 연동 로직은 추가하지 않는다 (필요하면 요청만 남기고 클로드 코드 쪽에 넘긴다).

## Git 작업 원칙
- 같은 파일을 동시에 건드리지 않는 것이 최우선.
- 다른 쪽(클로드 코드) 커밋이 이미 브랜치에 있으면 강제로 덮어쓰지 말고 merge로 합친다.
- `claude/robot-edu-app-scaffold-i5lhtn`은 브랜치 보호 규칙이 걸려있어 직접 push가 불가능하다.
  작업이 끝나면 반드시 **PR을 생성**해서 base를 `claude/robot-edu-app-scaffold-i5lhtn`으로 지정한다
  (승인 없이도 merge 가능하도록 설정되어 있음).
- 작업 시작 전에 `app/layout.tsx`, `app/globals.css` 등 공유 파일의 **현재 내용을 먼저 확인**하고,
  이미 들어가 있는 스타일(그라데이션 배경, 버튼 인터랙션 등)을 리셋하지 말고 그 위에 이어서 작업한다.
  (오래된 브랜치 기준으로 작업하면 병합 충돌이 남)
