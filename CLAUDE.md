# 작업 범위 (클로드 코드 전담)

이 저장소는 클로드 코드와 코덱스 두 AI가 나눠서 작업한다.
**나(클로드 코드)의 역할은 엔지니어다.** 데이터/로직/서버가 어떻게 동작하는지만 책임지고,
화면이 어떻게 보이는지(디자인 역할)는 코덱스가 담당한다.

## 클로드 코드가 다루는 영역
- `app/api/**` — API 라우트
- `lib/**` — DB 클라이언트, 세션, 보드 프로파일 헬퍼, 데이터 훅(`lib/hooks/**`)
- `prisma/**` — 스키마, 마이그레이션, 시드
- `board-profiles/**`

## 클로드 코드가 손대지 않는 영역
- `app/globals.css`, `tailwind.config.ts` — 전역 스타일/디자인 토큰은 코덱스 담당
- `public/**` — 이미지 등 정적 에셋
- 컴포넌트/페이지(`.tsx`)의 **className, 마크업 구조, 색상·간격·아이콘 같은 시각 요소** — 이건 코덱스가 다듬는 영역이므로 스타일링 목적만으로 임의로 바꾸지 않는다.

## 공유 파일(`.tsx`) 작업 방식
1. 클로드 코드는 데이터 훅(`lib/hooks/*`)을 만들고, 그 훅을 사용하는 최소한의 JSX 뼈대(기본 Tailwind 클래스, 껍데기 수준)를 컴포넌트/페이지에 연결한다.
2. 코덱스가 그 위에서 시각적으로 다듬는다 (className, 레이아웃, 아이콘/이미지).
3. 클로드 코드가 같은 파일을 다시 수정해야 할 때는 `onClick`/`useState`/`fetch`/props 같은 동작 관련 코드만 바꾸고, 코덱스가 입힌 className/마크업은 그대로 보존한다. 애매하면 먼저 물어본다.

## Git 작업 원칙
- 같은 파일을 동시에 건드리지 않는 것이 최우선. 겹치는 파일이 있으면 push 전에 반드시 확인.
- 다른 쪽(코덱스) 커밋이 이미 브랜치에 있으면 강제로 덮어쓰지 말고 merge로 합친다.
- `claude/robot-edu-app-scaffold-i5lhtn`은 브랜치 보호 규칙(PR 필수, 승인 불필요)이 걸려있어 직접 push가 막혀있다.
  작업 흐름: (1) 별도 브랜치(예: `claude/patch-*`)에 커밋 후 push → (2) 이 브랜치를 대상으로 PR 생성 →
  (3) 승인 없이 바로 merge 가능하니 그대로 merge까지 진행한다.
